# Encrypt this file with `sops --encrypt` and commit the resulting
# `secrets.sops.yaml` to keep sensitive variables in Git without exposing
# them in clear text.
#
# Usage:
#   cp inventory/group_vars/vps/secrets.sops.yaml.example \
#      inventory/group_vars/vps/secrets.sops.yaml
#   sops --encrypt --in-place inventory/group_vars/vps/secrets.sops.yaml
#   sops inventory/group_vars/vps/secrets.sops.yaml
#
# Populate the keys below with real values before encrypting.
---
vps_external_dns_api_token: REPLACE_WITH_CLOUDFLARE_TOKEN
vps_keycloak_admin_password: REPLACE_WITH_STRONG_PASSWORD
